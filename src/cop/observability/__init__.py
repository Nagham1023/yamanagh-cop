"""Trace (operational log), evals (audit), cost (tokens), GUI, replay verifier.

Seeded minimally in PRD 2 (just `trace.py`, the fifth wire the Orchestrator
needs per the book's Fig. 12); PRD 7 extends this package with the rest.
"""
